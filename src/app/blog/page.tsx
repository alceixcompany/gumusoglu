import Image from 'next/image';
import Link from 'next/link';
import { FiArrowRight, FiClock } from 'react-icons/fi';
import SitePageHero from '@/components/SitePageHero';
import { getPublishedPosts } from '@/lib/publicContent';
export default async function BlogPage(){const posts=await getPublishedPosts();return <main><SitePageHero eyebrow="Blog" title="Elektriğe dair doğru bilgiler." description="Güvenli tesisat, doğru malzeme seçimi ve proje süreçleri hakkında anlaşılır, faydalı içerikler." image="/gumusoglu/project-planning.webp"/><section className="px-5 py-20 sm:px-8 lg:px-14 lg:py-28"><div className="mx-auto max-w-[1500px]"><div className="grid gap-6 lg:grid-cols-3">{posts.map(post=><Link href={`/blog/${post.slug}`} key={post.slug} className="group overflow-hidden rounded-[26px] border border-[var(--line)] bg-white transition hover:-translate-y-1 hover:shadow-xl"><div className="relative min-h-[260px]"><Image src={post.image} alt={post.title} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width:1024px) 100vw,33vw"/></div><div className="p-7"><div className="flex items-center justify-between text-[10px] font-black uppercase tracking-[.14em] text-[var(--muted)]"><span className="text-[#e30613]">{post.category}</span><span className="flex items-center gap-2"><FiClock/>{post.readTime}</span></div><h2 className="mt-5 text-2xl font-black leading-tight">{post.title}</h2><p className="mt-4 text-sm leading-7 text-[var(--muted)]">{post.excerpt}</p><span className="mt-6 inline-flex items-center gap-2 text-sm font-black text-[#e30613]">Devamını okuyun <FiArrowRight/></span></div></Link>)}</div></div></section></main>}
