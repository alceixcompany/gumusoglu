import Image from 'next/image';
import Link from 'next/link';
import { FiArrowRight } from 'react-icons/fi';
import SitePageHero from '@/components/SitePageHero';
import { getPublishedGallery } from '@/lib/publicContent';

export default async function GalleryPage(){const gallery=await getPublishedGallery();return <main><SitePageHero eyebrow="Galeri" title="İşimizin içinden kareler." description="Planlamadan uygulamaya, malzeme tedariğinden son kontrole kadar çalışma alanlarımızdan seçkiler." image="/gumusoglu/project-planning.webp"/><section className="px-5 py-20 sm:px-8 lg:px-14 lg:py-28"><div className="mx-auto max-w-[1500px]"><div className="grid gap-6 lg:grid-cols-12">{gallery.map((item,i)=><figure key={item.id} className={`group relative overflow-hidden rounded-[28px] ${i===0?'min-h-[560px] lg:col-span-7':'min-h-[360px] lg:col-span-5'}`}><Image src={item.src} alt={item.title} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width:1024px) 100vw,60vw"/><div className="absolute inset-0 bg-gradient-to-t from-[#173b59]/75 via-transparent to-transparent"/><figcaption className="absolute inset-x-0 bottom-0 p-7 text-white"><span className="text-[10px] font-black uppercase tracking-[.18em] text-white/60">{item.tag}</span><h2 className="mt-2 text-2xl font-black">{item.title}</h2>{item.description&&<p className="mt-3 max-w-xl text-sm leading-6 text-white/70">{item.description}</p>}</figcaption></figure>)}</div><div className="mt-14 flex flex-col items-center rounded-[26px] bg-[#eef5fa] p-8 text-center sm:p-12"><h2 className="text-3xl font-black">Projeniz bir sonraki çalışmamız olsun.</h2><p className="mt-3 text-sm text-[var(--muted)]">Keşif ve ön değerlendirme için bizimle iletişime geçin.</p><Link href="/iletisim" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[#347bb7] px-6 py-4 text-sm font-black text-white">İletişime geçin <FiArrowRight/></Link></div></div></section></main>}
