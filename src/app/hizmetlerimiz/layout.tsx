import type { Metadata } from 'next';
export const metadata:Metadata={title:'Hizmetlerimiz',description:'Elektrik malzemeleri, tesisat, taahhüt, plan, proje, abonelik ve danışmanlık hizmetleri.',alternates:{canonical:'/hizmetlerimiz'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
